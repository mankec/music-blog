// import { redirect } from '@sveltejs/kit';
// import type { PageLoad } from '@sveltejs/kit'

// export const load: PageLoad = ({ session, data: props }) => {
//   if (session.user) {
//     throw redirect(302, '/mb');
//   }

//   return props
// }
