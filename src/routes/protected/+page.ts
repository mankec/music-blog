// import { redirect } from '@sveltejs/kit';
// import type { PageLoad } from '@sveltejs/kit'

// // import { base } from '$app/paths'

// export const load: PageLoad = ({ session }: any) => {
//   if (!session.user) {
//     throw redirect(302, 'mb/home');
//   }

//   return {
//     user: session.user.username
//   }
// }
